extends Node
var small_room_size = 200
var score = 0
var all_rooms = ["clown", "witches lair", "swamp", "forest", "classroom", "operating room"]
var current_rooms = ["clown", "witches lair", "swamp", "forest", "classroom", "operating room"]
var available_rooms = ["clown", "witches lair", "swamp", "forest", "classroom", "operating room"]


# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
